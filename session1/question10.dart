//Explain what happens if you try to use a variable before it is declared in Dart.

//the solution:

// In Dart, if a variable is used before it is declared, it will result in a compile-time error.
// Because Dart will not be able to infer its type, the program will fail to compile it.